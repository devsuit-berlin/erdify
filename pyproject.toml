[project]
name = "erdify"
version = "0.1.0"
description = "Generate PlantUML ERD diagrams from SQLModel models"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Fabian Clemenz", email = "fabian.clemenz@devsuit.de" }
]
requires-python = ">=3.10"
keywords = ["sqlmodel", "erd", "plantuml", "diagram", "database", "erdify"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Database",
    "Topic :: Software Development :: Code Generators",
]
dependencies = []  # No runtime dependencies - uses stdlib ast module

[project.optional-dependencies]
dev = ["pytest", "pytest-cov", "ruff", "mypy", "sqlmodel"]

[dependency-groups]
dev = ["pytest", "pytest-cov", "ruff", "mypy", "sqlmodel"]

[project.scripts]
erdify = "erdify.cli:main"

[project.urls]
Homepage = "https://github.com/devsuit-berlin/erdify"
Repository = "https://github.com/devsuit-berlin/erdify"
Documentation = "https://github.com/devsuit-berlin/erdify#readme"

[build-system]
requires = ["uv_build>=0.9.28,<0.11.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = ["tests/fixtures/malformed/"]

[tool.mypy]
python_version = "3.10"
strict = true
